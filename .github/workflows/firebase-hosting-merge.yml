# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "export const FIREBASE_DATABASE_URL = '${{ secrets.FIREBASE_DATABASE_URL }}';" > config.js && echo "export const APP_CHECK_RECAPTCHA_SITE_KEY = '${{ secrets.APP_CHECK_RECAPTCHA_SITE_KEY }}';" >> config.js && npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_GROCERY_LIST_APP_AB9DB }}
          channelId: live
          projectId: grocery-list-app-ab9db
